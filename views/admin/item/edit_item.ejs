<div class="tab-pane fade show active" id="edit-item" role="tabpanel" aria-labelledby="add-tab">
  <div class="card shadow mb-4 mt-2">
    <div class="card-body">
      <form action="/admin/item/<%= item.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <div class="from-group">
          <label for="title">Nama</label>
          <input type="text" class="form-control" id="title" name="title" value="<%= item.title %>" placeholder="Masukkan nama" required />
        </div>
        <div class="from-group">
          <label for="price">Harga</label>
          <input type="number" class="form-control" id="price" name="price" value="<%= item.price %>" placeholder="Masukkan harga" required />
        </div>
        <div class="from-group">
          <label for="categoryId">Category</label>
          <select class="form-control" name="categoryId" id="categoryId">
            <option value="<%= item.categoryId._id %>">--- <%= item.categoryId.name %> ---</option>
            <% for(let i = 0; i < category.length; i++) { %>
            <option value="<%= category[i].id %>"><%= category[i].name %></option>
            <% } %>
          </select>
        </div>
        <div class="from-group mb-2">
          <label for="image">Gambar</label>
          <input type="file" class="form-control" id="image" name="image" multiple />
        </div>
        <button type="submit" class="btn btn-primary">Simpan</button>
      </form>
    </div>
  </div>
</div>
